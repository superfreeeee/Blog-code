spring:
  application:
    name: config-client
  cloud:
    config:
      fail-fast: true # 获取不到配置时立即失败
      profile: prod # prod 环境
      discovery:
        enabled: true
        service-id: config-server
