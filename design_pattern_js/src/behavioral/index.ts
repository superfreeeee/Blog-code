import { group } from '../utils/console'

group('Chain of Responsibility', () => {
  require('./chain_of_responsibility')
})

group('Command', () => {
  require('./command')
})

group('Interpreter', () => {
  require('./interpreter')
})

group('Iterator', () => {
  require('./iterator')
})

group('Mediator', () => {
  require('./mediator')
})

group('Memento', () => {
  require('./memento')
})

group('Observer', () => {
  require('./observer')
})

group('State', () => {
  require('./state')
})

group('Strategy', () => {
  require('./strategy')
})

group('Template Method', () => {
  require('./template_method')
})

group('Visitor', () => {
  require('./visitor')
})
