/**
 * 导出 expr.match.needsContext 函数
 *   即 Sizzle.selectors.match.needsContext
 */
// ? Read
define( [
	"../../core",
	"../../selector"
], function( jQuery ) {
	"use strict";

	return jQuery.expr.match.needsContext;
} );
