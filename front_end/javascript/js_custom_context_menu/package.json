{
  "name": "js_context_menu",
  "description": "front end application by pure HTML + CSS + JS",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "nodemon --exec 'http-server'",
    "start:yx": "nodemon --exec 'serve'",
    "reinstall": "yarn clean && yarn install",
    "clean": "rm -r node_modules || true"
  },
  "dependencies": {},
  "devDependencies": {
    "@youxian/simple-server": "^0.1.1",
    "http-server": "^14.0.0",
    "nodemon": "^2.0.15"
  },
  "author": "superfree"
}
