import styled from 'styled-components';

const Container = styled.div`
  max-width: 900px;
  min-height: 600px;
  padding: 80px 70px 90px;
  margin: 40px auto 0;
  border-radius: 25px;
  box-shadow: 0 15px 25px -15px var(--box_shadow_color);
  background-color: #fff;
`;

export default Container;
