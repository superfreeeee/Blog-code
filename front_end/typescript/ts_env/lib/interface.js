import './basic';
console.log('----- interface -----');
const p = { id: 1, name: 'John' };
const p2 = { id: 2, name: 'Penny', label: '可选属性' };
function area(s) {
    return s.width * s.height;
}
const s = { width: 200, height: 40 };
export default null;
