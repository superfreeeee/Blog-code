const inquirer = require('inquirer')

/***** build web questions *****/
const buildWebProject = async ({ name, author, type }) => {
  console.log('build web')
}

module.exports = {
  buildWebProject,
}
